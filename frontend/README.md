# OPC Dashboard Frontend

Stub. Will be initialized with Vite + React + TypeScript.

```bash
npm create vite@latest . -- --template react-ts
npm install cytoscape react-cytoscapejs
```

Talks to the backend at `/api`. Configure base URL and bearer token via env (`VITE_API_BASE`, `VITE_API_TOKEN`).
